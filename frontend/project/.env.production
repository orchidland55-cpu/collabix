# ===========================================
# Collabix Frontend - Production Build
# Used automatically by `vite build` (mode production).
# NOTE: update VITE_API_BASE_URL with the real Railway backend URL
# after the backend is deployed (Railway domain is generated at deploy,
# e.g. https://<service>.up.railway.app or your custom domain).
# ===========================================

VITE_API_BASE_URL=https://collabix-production-eead.up.railway.app/api
VITE_API_TIMEOUT=30000
