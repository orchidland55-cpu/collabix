# ===========================================
# Collabix Frontend - Production Build
# Used automatically by `vite build` (mode production).
# NOTE: update VITE_API_BASE_URL with the real Render backend URL
# after the backend is deployed (replace XXXXX with the actual subdomain).
# ===========================================

VITE_API_BASE_URL=https://collabix-backend-XXXXX.onrender.com/api
VITE_API_TIMEOUT=30000
